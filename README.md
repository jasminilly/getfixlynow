# Fixly Website

GitHub Pages-ready static site.

Upload the contents of this folder to the root of your GitHub repository:
- `index.html`
- `assets/`

Then enable GitHub Pages from the repository's `main` branch and `/ (root)`.
